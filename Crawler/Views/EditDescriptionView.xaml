<Window x:Class="Crawler.Views.EditDescriptionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}" Height="500" Width="480" MinHeight="500" MinWidth="480"
        ResizeMode="CanResize" WindowStyle="ToolWindow"
        Loaded="EditDescriptionView_OnLoaded"
        >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="360"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Image x:Name="ImageThumb" Stretch="None" Source="{Binding LargeThumb, IsAsync=True}"></Image>
        <TextBox Grid.Row="1" Text="{Binding Description}" />
    </Grid>
</Window>
