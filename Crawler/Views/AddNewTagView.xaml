<Window x:Class="Crawler.Views.AddNewTagView" x:Name="window"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:behaviors="clr-namespace:Crawler.Behaviors"
        Title="Tag" Height="55" Width="300" WindowStyle="ToolWindow"
        DataContext="{Binding Path=AddNewTagViewModel, Source={StaticResource VmLocator}}"
        >
    <StackPanel Orientation="Horizontal">
        <TextBox Width="250" behaviors:TextBoxBehavior.IsFocused="True"
                 Margin="1"
                 Text="{Binding Tag.Title, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged}"
                 />
        <Button Width="30" Margin="2,1,1,1" Content="OK" 
                Command="{Binding AddTagCommand, Mode=OneTime}" CommandParameter ="{Binding ElementName=window}" 
                x:Name="buttonOk"
                />
    </StackPanel>

</Window>